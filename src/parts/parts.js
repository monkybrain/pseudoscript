// Generated by CoffeeScript 1.10.0
(function() {
  var actions, adverbs, i, j, k, key, keywords, l, len, len1, len2, len3, m, modules, prep, preps, ref, ref1, synonym, value, verb, verbs;

  verbs = require("./verbs/verbs");

  adverbs = require("./adverbs/adverbs");

  modules = require("./../modules/modules");

  keywords = [];

  for (i = 0, len = verbs.length; i < len; i++) {
    verb = verbs[i];
    ref = verb.lexical.synonyms;
    for (j = 0, len1 = ref.length; j < len1; j++) {
      synonym = ref[j];
      keywords.push(synonym);
    }
  }

  actions = [];

  for (k = 0, len2 = modules.length; k < len2; k++) {
    m = modules[k];
    ref1 = m.actions;
    for (key in ref1) {
      value = ref1[key];
      keywords.push(key);
    }
  }

  preps = ["after", "in", "wait", "in", "every", "when"];

  for (l = 0, len3 = preps.length; l < len3; l++) {
    prep = preps[l];
    keywords.push(prep);
  }

  module.exports = {
    adverbs: adverbs,
    verbs: verbs,
    keywords: keywords
  };

}).call(this);
