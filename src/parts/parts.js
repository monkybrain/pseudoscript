// Generated by CoffeeScript 1.10.0
(function() {
  var Add, Adverb, Get, Set, adverb, adverbs, dict, i, j, k, keywords, len, len1, len2, ref, synonym, verb, verbs;

  dict = require("../dictionaries/base");

  Adverb = require("./adverb");

  Set = require("./verbs/set");

  Get = require("./verbs/get");

  Add = require("./verbs/add");

  verbs = [Add, Get, Set];

  keywords = [];

  for (i = 0, len = verbs.length; i < len; i++) {
    verb = verbs[i];
    ref = verb.lexical.synonyms;
    for (j = 0, len1 = ref.length; j < len1; j++) {
      synonym = ref[j];
      keywords.push(synonym);
    }
  }

  adverbs = ["after", "in", "wait", "in"];

  for (k = 0, len2 = adverbs.length; k < len2; k++) {
    adverb = adverbs[k];
    keywords.push(adverb);
  }

  module.exports = {
    adverb: new Adverb(dict),
    verbs: verbs,
    keywords: keywords
  };

}).call(this);
