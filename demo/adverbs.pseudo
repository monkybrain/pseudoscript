# Add a light called 'ceiling light' and after two minute set the brightness to 20
# Add light 'ceiling light', wait 2 minute, set brightness 20
Wait 3 milliseconds
# After two minutes set the brightness to 20
# Vänta två minuter and set the brightness to 20
