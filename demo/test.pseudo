Add a room called 'bedroom'

Add a button

Add a light called 'test' and set the brightness to 20

When the button is pushed, set the color to blue and after 1 second, set the color back to red

When the 'bedroom' gets dark, set the brightness to 200

Upon light in the 'bedroom', set the brightness to 20