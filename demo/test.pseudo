Add a light and set the brightness to 10

Add a button

After 1 second, turn the brightness of the light up to 100 and set the color to red

Every 5 seconds, set the color to random

When the button is pushed, set the color to blue